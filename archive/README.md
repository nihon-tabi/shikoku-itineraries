Superseded. Kept only so nothing written by hand is lost.

`ITINERARIES.md` was the first prose draft of the itineraries, written before the
generators existed. Everything in it now lives in `src/itineraries.py` and is
rendered into `out/`. It is NOT kept in sync — read the generated deliverables
instead, and never copy a figure out of here.
